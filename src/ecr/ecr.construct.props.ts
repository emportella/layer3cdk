import { BaseConstructProps } from '../core/base.construct.props';

/**
 * Props for {@link ApplicationRepository.create} factory method.
 */
export interface ApplicationRepositoryProps extends BaseConstructProps {
  repositoryName: string;
}
